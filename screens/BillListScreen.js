import React from 'react'

import { Text, View } from "react-native";

export const BillListScreen = ({ navigation }) => {
    return (
      <View style={{ flex: 1, backgroundColor:'#fff', alignItems: "center", justifyContent: "center" }}>
        <Text>Bill List Screen</Text>
        
      </View>
    );
  };