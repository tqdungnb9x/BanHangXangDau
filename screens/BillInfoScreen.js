import React from 'react'

import { Text, View } from "react-native";

export const BillInfoScreen = ({ navigation }) => {
    return (
      <View style={{ flex: 1, backgroundColor:'#fff', alignItems: "center", justifyContent: "center" }}>
        <Text>Bill Info Screen</Text>
        
      </View>
    );
  };
